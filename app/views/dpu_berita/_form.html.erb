<%= form_for(@dpu_beritum) do |f| %>
  <% if @dpu_beritum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dpu_beritum.errors.count, "error") %> prohibited this dpu_beritum from being saved:</h2>

      <ul>
      <% @dpu_beritum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, "Judul" %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content, "Isi" %><br>
    <%= f.text_area :content %>
  </div>
  
 
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
