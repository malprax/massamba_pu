<div class="wrap">
  <%= render "shared/navigation" %>
  <div class="container"> 
    <table class="table">
      <thead>
        <tr>
          <th>No</th>
          <th>Berita Dinas Pekerjaan Umum</th>        
          <th></th>
          <th></th>
          <th></th>
        </tr>
      </thead>

      <tbody>
        <% @dpu_berita.each_with_index do |dpu_beritum, i| %>
          <tr>
            <td><%= i = i + 1 %></td>
            <td><%= link_to dpu_beritum.title, dpu_beritum %></td>      
            <td><%= link_to 'Edit', edit_dpu_beritum_path(dpu_beritum) %></td>
            <td><%= link_to 'Hapus', dpu_beritum, method: :delete, data: { confirm: 'Apakah Anda Yakin?'} %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <br>

    <%= link_to 'Buat Berita DPU', new_dpu_beritum_path %> || <%= link_to 'Menu Admin »', admin_path %>
  </div>
</div>
<%= render 'shared/footer' %>

