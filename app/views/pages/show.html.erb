   
      <!-- Begin page content -->
      <div class="wrap">
        <%= render "shared/navigation" %>
        <div class="container">
          <div class="page-header">
            <h1><%= @page.name %></h1>
          </div>
          <p class="lead">raw <%= @page.content %></p>          
        </div>

        <div id="push">
          <div class="container">
            <% if params[:edit] %>
            <%= link_to 'Edit', edit_page_path(@page) %> |
            <%= link_to 'Kembali »', admin_path %> 
            <% end %>   
          </div>              
        </div>
      </div>      
      
      <%= render "shared/footer" %>
      

