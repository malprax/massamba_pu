<%= form_for(@suara_rakyat) do |f| %>
  <% if @suara_rakyat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@suara_rakyat.errors.count, "error") %> prohibited this suara_rakyat from being saved:</h2>

      <ul>
      <% @suara_rakyat.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_field :address %>
  </div>
  <div class="field">
    <%= f.label :categori_suara_id %><br>
    <%= f.collection_select(:categori_suara_id, CategoriSuara.order("created_at asc"), :id, :jenis_suara)  %>
  </div>
  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
